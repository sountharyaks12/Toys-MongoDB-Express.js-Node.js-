<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="theme-color" content="#1bb566">
    <title>Edit Toy</title>
    <link rel="stylesheet" href="/edit.css">
</head>

<body>
    <div class="container">
        <div class="form-wrapper">
            <h1 class="title">Edit Toy Details</h1>
            <form action="/toys/edit/<%= toy._id %>" method="POST" class="edit-form">
                <div class="form-group">
                    <label for="name">Name</label>
                    <input type="text" id="name" name="name" value="<%= toy.name %>" required>
                </div>
                <div class="form-group">
                    <label for="price">Price</label>
                    <input type="number" id="price" name="price" value="<%= toy.price %>" required>
                </div>
                <div class="form-group">
                    <label for="description">Description</label>
                    <input type="text" id="description" name="description" value="<%= toy.description %>">
                </div>
                <div class="form-group">
                    <label for="stock">Stock</label>
                    <input type="number" id="stock" name="stock" value="<%= toy.stock %>">
                </div>
                <div class="buttons">
                    <a href="/toys" class="backLink">Back to Toy List</a>
                    <button type="submit" id="updateBtn">Update Toy</button>
                </div>
            </form>
        </div>
    </div>
</body>
</html>
